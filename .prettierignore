# Markdown — Prettier's markdown formatter mangles snake_case identifiers
# and wildcards in technical prose (e.g., `entity_type` → `entity*type`,
# `belief_*_wavg` → `belief*\*\_wavg`). Docs in this repo carry a lot of
# schema references, so we leave markdown to the author's own formatting.
**/*.md

# Build output
dist/
.aws-sam/
coverage/

# Generated data artifacts
map-data.json
map-detail.json
backup-*.json
backup-*.sql
enriched-elections.json

# Dependencies
node_modules/
package-lock.json

# Lockfiles and binary data
*.min.js
*.min.css

# OS files
.DS_Store

# Don't reformat third-party notebooks or XLSX dumps
*.xlsx

# Dogfood / test output
dogfood-output/
test-screenshots/
