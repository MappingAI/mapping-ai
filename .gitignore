node_modules/
.env
.env.*
.vercel/
.vercel
.aws-sam/
submissions/
exports/

# Dev-only tools
dev-server.js
scripts/exa-pilot.js
scripts/extract-fields.js

# Enrichment output (generated, contains raw API data)
enriched-elections.json

# Data files generated from the live DB during deploy — only the repo-root
# copies are gitignored. `fixtures/map-data.json` + `fixtures/map-detail.json`
# are the committed fork-UX fallback (see fixtures/README.md).
/map-data.json
/map-detail.json
backup-*.json
backup-*.sql
scripts/seed-test-data.js
scripts/calc-disagreement.js
scripts/export-pending.js

# External data CSVs (stored locally, not in repo)
data/AISafety.com_map.csv

# OS files
.DS_Store
*.swp

# Claude Code local files
.claude/

# Build output (Cloudflare Pages preview)
dist/

# Testing output
dogfood-output/
test-screenshots/

# Local notes
slack-update.md
launch-prep.md

